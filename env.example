APP_PORT=4000

### These variables are needed to make script for getting access_token works 
AUTH_USERNAME=yourGithubLogin ### It should be your github username
AUTH_PASSWORD=TEST_PASSWORD
APP_URL=http://localhost:4000
###

### Database Configuration (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=cartadmin
DB_PASSWORD=password
DB_NAME=cartdb
NODE_ENV=development

### AWS Configuration (automatically set when deployed to AWS Lambda)
# DB_SECRET_ARN=<secret-arn> (automatically set by CDK)
# DB_HOST=<rds-endpoint> (automatically set by CDK)
# DB_PORT=5432 (automatically set by CDK)
# DB_NAME=cartdb (automatically set by CDK)